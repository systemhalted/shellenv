# Task Log

- Added `docs/ARCHITECTURE.md` outlining layout, key packages, command flows, and env variables; expanded with Cobra and exec resolution details.
- Updated `README.md` with links to documentation and a disclaimer (use at own risk, no warranties).
- Added MIT `LICENSE` to align with the warranty disclaimer.
- Added `ExecuteWithArgs` helper for CLI tests and covered `cmd/shellenv` with help/list tests.
- Added broad CLI unit coverage, gitignore for build artifacts, and documented running `go test` with a repo-local cache when permissions are restricted.
- Documented a short, step-by-step snippet for using shellenv inside a user's project directory (create/use/activate/exec).
- Expanded README usage section with safety notes explaining what each command does and that it stays within the project/temp home.
- Clarified `SHELLENV_HOME` setup with both mktemp-based and manual directory options for environments without `mktemp`.
- Added `CONTRIBUTING.md` with contributor workflow, testing commands, coding style, doc ownership, and safety guidance; linked from README.
- Recorded this task log in `docs/Task.md`.
- Expanded `docs/ARCHITECTURE.md` with an honest Isolation Model (PATH-shimming, boundary table, intended use vs non-use) and a Limitations section enumerating the P0/P1/P2 gaps.
- Added `docs/DESIGN.md` capturing eight design decisions (rationale/trade-offs/status) and a gap-closing Roadmap (R1–R6); linked it from `README.md`.
- R1: `exec` now isolates `HOME`/`TMPDIR`/`XDG_*` to a per-env sandbox (`./.shellenv/<env>/home/`); added `project.SandboxHomeDir` and an `upsertEnv` helper.
- R2: added `exec --profile` (opt-in), which sources the env's declared profile through the declared shell (`profileShell`) before running the command.
- Added exec tests for HOME/TMPDIR isolation and profile sourcing; updated ARCHITECTURE/DESIGN/README to match.
- R2 polish: silenced Cobra's usage/duplicate-error dump (`SilenceUsage`/`SilenceErrors`); `exec` now propagates a child's real exit code via an `exitError` type instead of always exiting 1, and runtime errors print a single clean line.
- Added `--container <image>` flag to `shellenv exec` to run commands inside Docker or Podman containers. Implemented TTY detection, volume mounts for the workspace, working directory mapping, environment variable forwarding, and entrypoint wrapping via `sh -c`. Added unit and integration tests, and updated documentation.
- Installed Go 1.26.4 (user-local, `~/.local/go`) and bats 1.13; verified and committed the pending `--container` work after running the full unit + integration suites.
- R3: `activate`/`exec` now resolve the declared `<shell>@<version>` to `$SHELLENV_HOME/installs/<shell>/<version>/bin` (new `env.ParseShellVersion`/`env.ResolveRuntime`) and prepend it to PATH after the env `bin/`. Declared-but-missing runtimes warn on stderr and fall back to the system shell; new `--strict-shell` flag on both commands errors instead. Container mode skips resolution and rejects `--strict-shell`. `install`/`uninstall` now share `ParseShellVersion`. Added unit tests (env + cli), two bats tests, and updated README/ARCHITECTURE/DESIGN (decision 11, R3 done).
- R4: removed the unused `$SHELLENV_HOME/shims` directory and its references (`env.ShimsDir`, `init`'s rc-file PATH hint, `doctor`'s Shims line); per-env PATH pinning from R3 supersedes global shims (DESIGN decision 12, R4 done). Also listed `docs/DESIGN.md` in CONTRIBUTING's doc-ownership list.
- R6: fish activation now sources `<profile>.fish` variants (`ResolveProfileForShell`, no `.sh` fallback; built-ins added under `profiles/`); added `exec --ephemeral` (throwaway sandbox home under the env dir, removed after the run, works in container mode); corrupt `metadata.json` warns on stderr via a shared `loadMetadata` helper (missing file stays silent); added isolation-breach tests (XDG writes land in sandbox, ephemeral teardown on success/failure, `activate` stdout never overrides HOME/TMPDIR/XDG) and an `--ephemeral` bats test. DESIGN R6 marked done; ARCHITECTURE limitations updated.
- R5: replaced placeholder installers with real source builds via new `internal/installer` (download official tarball to `$SHELLENV_HOME/cache/` → SHA-256 verify against checksums pinned from the upstream artifacts for bash@5.2/zsh@5.9, warn for unpinned versions, fail+purge on mismatch → `configure`/`make -j`/`make install` in `$SHELLENV_HOME/tmp/build-*/` with `build.log`, kept on failure). Preflight requires cc/gcc+make+tar; `doctor` reports toolchain status; unsupported shells error naming bash/zsh. zsh downloads use the SourceForge mirror (zsh.org/pub 404s). Unit tests stub network/exec; opt-in bats test (`SHELLENV_TEST_REAL_INSTALL=1`) verified a real bash 5.2 build end-to-end. DESIGN decision 13, R5 done; decision 8 superseded.
- CI: added GitHub Actions — `ci.yml` (gofmt gate, vet, unit tests, `make build`, full bats suite with temp `SHELLENV_HOME` on push/PR to main; the docker container test runs on the ubuntu runners) and `real-install.yml` (weekly cron + manual dispatch, runs the suite with `SHELLENV_TEST_REAL_INSTALL=1` and `build-essential`).
- Versioning + releases: added `shellenv --version` (cobra `rootCmd.Version` fed by an ldflags-stamped `internal/cli.version`, `dev` when unstamped; Makefile stamps `git describe --tags --always --dirty`), and `release.yml` — on `v*` tags, cross-compiles linux/darwin × amd64/arm64 tarballs each bundling `profiles/` beside the binary (required for profile resolution), generates SHA256SUMS, publishes via action-gh-release. README gained an "Install from a release" section.
- R7: added `activate --isolate-home` (redirects HOME/TMPDIR/XDG_* at the env sandbox via new `project.SandboxLayout`/`EnsureSandboxDirs`, which `exec` now also uses; the CLI pre-creates dirs so stdout stays pure shell code, and a stderr note points at the restore) and a new `shellenv deactivate` command (guard-everything snippet: whole-PATH/PS1 snapshot restore from write-once `SHELLENV_OLD_*` saves, per-var restore with empty→unset rule, unsets all `SHELLENV_*`; silent no-op when nothing is active, works for bash/zsh/fish). Real-shell round-trip tests (bash; fish skipped when absent) and a bats round-trip. DESIGN decision 14, R7 in roadmap; ARCHITECTURE isolation table and README updated.
- Debt cleanup: removed the `create --with-tools` busybox placeholder flag (the `Tools` metadata field stays as a schema slot); `uninstall` now warns on stderr when an env in the current directory still declares the removed version (best-effort — shellenv has no cross-project registry); `profiles/interactive.sh` gained real content (relax strictness, bash/zsh history niceties, `true` terminator so sourcing reports success) and `interactive.fish` quiets the greeting.
- Docs audit: retired the stale "PATH-shimming only" framing — README's scope note and ARCHITECTURE's isolation model now describe shellenv as a user-space sandbox (PATH/runtime pinning + HOME/TMPDIR/XDG redirection, container mode as the opt-in namespace escape hatch); ARCHITECTURE's mechanism list gained the missing HOME/TMPDIR/XDG entry and no longer claims "no containers" while listing container mode. DESIGN decisions 1/6/11 updated to reflect R5–R7 (boundary tightened, fish profile variants exist, installers are real). AGENTS.md: added `internal/installer`, listed `.fish` profile variants, dropped "no history exists yet", pointed release notes at GitHub Releases.
- Docs + robustness fixes: retitled DESIGN decision 8 to past-tense/superseded so a heading-scan isn't misled; README's intent bullet now states fish is activation/profiles-only (no installer); `create` propagates a failed `activate.sh` write instead of silently producing a broken env; new `install --require-checksum` refuses versions without a pinned SHA-256 before downloading (decision-11-style strict opt-in, amended into decision 13), and the unpinned warning advertises it.
- CI hardening: ci.yml runs a ubuntu+macos matrix (bats via apt/brew) so darwin binaries stop shipping untested; new ubuntu lint job runs `make lint` (gofmt + vet + staticcheck v0.7.0 via `go run`, pinned in the Makefile) and `make vulncheck` (govulncheck v1.6.0) on the newest stable Go — the pinned-1.22 line is EOL and would flag already-fixed stdlib vulns; release.yml gained a `test: uses: ci.yml` gate (`workflow_call`) and builds artifacts with stable Go so shipped binaries carry current stdlib security fixes (go.mod's 1.22 remains the source-build minimum, verified by the test job); the container bats test accepts docker or podman and fails instead of skipping when `SHELLENV_TEST_REQUIRE_CONTAINER=1` (set in CI on Linux) so the container path can't silently stop being tested.
- Advisory env registry (DESIGN decision 15): new `internal/registry` maintains `$SHELLENV_HOME/registry.json` (versioned JSON, `(root, name)`-keyed upserts, atomic temp+rename writes); `create`/`destroy` register/unregister best-effort (stderr warning, never an error — create works even with an unwritable registry); `uninstall` warns cross-project by re-validating entries against each project's `metadata.json` (never the cached shell field) and silently prunes vanished projects; `list --all` shows registered envs as `NAME\tSHELL\tROOT` plus unregistered cwd envs. Unit tests for the package and every CLI path, a `registry.bats` integration file, and a `SHELLENV_HOME` sweep of existing create/destroy tests so they can't touch the real `~/.shellenv`.
- CI fix: the macOS test leg now uses stable Go (matching the toolchain darwin release artifacts are built with) — Go 1.22.0 test binaries lack the `LC_UUID` load command that dyld on current `macos-latest` runners (macOS 26) hard-requires, aborting any test that re-execs itself; the Linux leg still verifies go.mod's declared 1.22 minimum.
- Developer-doc diagrams: ARCHITECTURE.md gained eight Mermaid diagrams (GitHub renders them natively) — a system-overview architecture diagram, a package/component diagram (the strict cli → siblings fan-out), an ER diagram relating the on-disk state (global home, runtimes, registry entries, project envs, metadata — with the two deliberately-weak advisory relationships dotted), sequence diagrams for activate / exec (host+container) / install / uninstall-with-registry, and a session-lifecycle state diagram for activate/deactivate. All blocks are syntax-validated with mermaid's own parser.

Tests: `GOCACHE=$(pwd)/.gocache go test ./...`.
